/*
 * 字体设置
 */
"use strict";

import React from 'react';

const Font = {
    big: { fontSize: 20, fontWeight: 'bold' }, // 大号黑体
    medium: { fontSize: 16, fontWeight: 'normal' },  // 中号字体
    small: { fontSize: 12 } // 小号字体
};

export default Font;